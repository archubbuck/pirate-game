import { Html } from "@react-three/drei";
import { useGameStore } from "@/lib/stores/useGameStore";

export function CollectionIndicator() {
  // Collection is now instant, so this component is no longer needed
  return null;
}